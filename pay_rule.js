
//INPUT PARAMENTERS
const payRate =17.30
const hoursworked =45

// OUTPUT RETURNED
let pay = 0

//IS REGULAR?
if(hoursworked <= 40){
    pay = payrate * hoursworked;
}else{
    standardpay =payRate * 40;
    overtimeHours = hoursWorked - 40
    overtimeRate = payRate * 1.5
    overTimePay = overtimeHours * overtimeRatepay
    pay = sandardpay + overTimePay
}

console.log(pay)
